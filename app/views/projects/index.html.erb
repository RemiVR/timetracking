<h1>Projects list</h1>
	<ul>
		<%@projects.each do |project|%>
			<h3><li><%= link_to(project.name, project_entries_path(project))%></h3>
				<% if project.description.present?%>
			-<%= project.description%>
			<%end%>
			</li>
		<%end%>
	</ul>
